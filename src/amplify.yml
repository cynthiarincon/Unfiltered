version: 1
applications:
  type: nodejs
  platform: WEB_COMPUTE
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .
    files:
      - "**/*"
  environment:
    variables:
      NODE_ENV: production
